<UserControl x:Class="ACGCET_Admin.Views.DeleteEntry.DeleteNccCourseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:ACGCET_Admin.ViewModels.DeleteEntry"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:DeleteNccCourseViewModel}"
             d:DesignHeight="700" d:DesignWidth="1024">
    <Grid Background="Transparent" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="3" Text="Reg No" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0 0 15 0"/>
            <TextBox Grid.Column="4" Text="{Binding RegNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            <Button Grid.Column="6" Content="View" Command="{Binding SearchCommand}"/>
        </Grid>
        <StackPanel Grid.Row="0" HorizontalAlignment="Right" Orientation="Horizontal">
             <Button Content="Delete" Command="{Binding DeleteCommand}" Background="Red" BorderBrush="Red" Margin="8 0"/>
             <Button Content="Clear" Command="{Binding ClearCommand}" Style="{StaticResource SubtleButton}"/>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding PaperList}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Paper Code" Binding="{Binding PaperCode}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Paper Name" Binding="{Binding PaperName}" IsReadOnly="True" Width="*"/>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
