<UserControl x:Class="ACGCET_Admin.Views.DeleteEntry.DeleteStudentMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:ACGCET_Admin.ViewModels.DeleteEntry"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:DeleteStudentMasterViewModel}"
             d:DesignHeight="700" d:DesignWidth="1024">

    <StackPanel Margin="16" MaxWidth="600" HorizontalAlignment="Left">

        <!-- Reg No row -->
        <Grid Margin="0 0 0 4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Reg No" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox   Grid.Column="1" Text="{Binding RegNo, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <Grid Margin="0 0 0 4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Student Name" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox   Grid.Column="1" Text="{Binding StudentName}" IsReadOnly="True"/>
        </Grid>

        <Grid Margin="0 0 0 4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Program" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox   Grid.Column="1" Text="{Binding ProgramName}" IsReadOnly="True"/>
        </Grid>

        <Grid Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Batch" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox   Grid.Column="1" Text="{Binding Batch}" IsReadOnly="True"/>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Orientation="Horizontal">
            <Button Content="VIEW"           Command="{Binding SearchCommand}" Style="{StaticResource PrimaryButton}" Margin="0 0 8 0"/>
            <Button Content="DELETE STUDENT" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}"  Margin="0 0 8 0"/>
            <Button Content="CLEAR"          Command="{Binding ClearCommand}"  Style="{StaticResource SubtleButton}"  Margin="0 0 8 0"/>
            <Button Command="{Binding RefreshCommand}" Style="{StaticResource IconButton}" ToolTip="Refresh">
                <md:PackIcon Kind="Refresh" Width="14" Height="14"/>
            </Button>
        </StackPanel>
    </StackPanel>
</UserControl>
