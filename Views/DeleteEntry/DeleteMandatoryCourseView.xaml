<UserControl x:Class="ACGCET_Admin.Views.DeleteEntry.DeleteMandatoryCourseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:ACGCET_Admin.ViewModels.DeleteEntry"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:DeleteMandatoryCourseViewModel}"
             d:DesignHeight="700" d:DesignWidth="1024">

    <Grid Background="Transparent" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Filters -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 8">
            <TextBlock Text="Reg No" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0 0 12 0"/>
            <TextBox Text="{Binding RegNo, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0 4"/>
        </StackPanel>

        <!-- Row 1: Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 12">
            <Button Content="VIEW"   Command="{Binding SearchCommand}" Style="{StaticResource PrimaryButton}" Margin="0 0 8 0"/>
            <Button Content="DELETE" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}"  Margin="0 0 8 0"/>
            <Button Content="CLEAR"  Command="{Binding ClearCommand}"  Style="{StaticResource SubtleButton}"  Margin="0 0 8 0"/>
        </StackPanel>

        <!-- Row 2: DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding PaperList}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Paper Code" Binding="{Binding PaperCode}" IsReadOnly="True" Width="160"/>
                <DataGridTextColumn Header="Paper Name" Binding="{Binding PaperName}" IsReadOnly="True" Width="*"/>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
