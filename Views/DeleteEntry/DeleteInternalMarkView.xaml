<UserControl x:Class="ACGCET_Admin.Views.DeleteEntry.DeleteInternalMarkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:ACGCET_Admin.ViewModels.DeleteEntry"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:DeleteInternalMarkViewModel}"
             d:DesignHeight="700" d:DesignWidth="1024">

    <Grid Background="Transparent" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Reg No" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Grid.Column="1" Text="{Binding RegNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            
            <Button Grid.Column="3" Content="View" Command="{Binding SearchCommand}"/>
            
            <TextBlock Grid.Column="4" Text="{Binding StudentName}" VerticalAlignment="Center" Margin="16 0" FontWeight="Bold"/> 
        </Grid>
        
        <StackPanel Grid.Row="0" HorizontalAlignment="Right" Orientation="Horizontal">
             <Button Content="Delete" Command="{Binding DeleteCommand}" Background="Red" BorderBrush="Red" Margin="8 0"/>
             <Button Content="Clear" Command="{Binding ClearCommand}" Style="{StaticResource SubtleButton}"/>
             <Button Command="{Binding RefreshCommand}" Style="{StaticResource IconButton}" ToolTip="Refresh" Margin="8 0 0 0">
                 <materialDesign:PackIcon Kind="Refresh" Width="14" Height="14"/>
             </Button>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding MarkList}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Paper Code" Binding="{Binding PaperCode}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Test Type" Binding="{Binding TestName}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Mark" Binding="{Binding Mark}" IsReadOnly="True"/>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
