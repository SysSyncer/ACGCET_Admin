<UserControl x:Class="ACGCET_Admin.Views.AdminControl.DataInputLockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:ACGCET_Admin.ViewModels.AdminControl"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:DataInputLockViewModel}"
             d:DesignHeight="700" d:DesignWidth="800">

    <Grid Background="Transparent" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Data Input Lock Configuration" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0 0 0 16"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding LockItems}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Module Name" Binding="{Binding ModuleName}" IsReadOnly="True" Width="*"/>
                <DataGridCheckBoxColumn Header="Locked" Binding="{Binding IsLocked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 16 0 0">
            <Button Content="UPDATE LOCKS" Command="{Binding UpdateLocksCommand}" Width="150" Style="{StaticResource PrimaryButton}"/>
            <Button Content="CLEAR" Command="{Binding ClearCommand}" Width="100" Margin="16 0 0 0" Style="{StaticResource SubtleButton}"/>
            <Button Command="{Binding RefreshCommand}" Style="{StaticResource IconButton}" ToolTip="Refresh" Margin="8 0 0 0">
                <materialDesign:PackIcon Kind="Refresh" Width="14" Height="14"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
