<UserControl x:Class="ACGCET_Admin.Views.AdminControl.NewUserCreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:ACGCET_Admin.ViewModels.AdminControl"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:NewUserCreationViewModel}"
             d:DesignHeight="700" d:DesignWidth="800">

    <Grid Background="Transparent" Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Form -->
        <StackPanel Grid.Column="0" Margin="0 0 24 0">
            <TextBlock Text="User Details" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0 0 0 16"/>

            <TextBox materialDesign:HintAssist.Hint="User Name" Text="{Binding Username}" Margin="0 8" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            
            <PasswordBox x:Name="pbPassword" materialDesign:HintAssist.Hint="Password" Margin="0 8" Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         PasswordChanged="PbPassword_PasswordChanged"/>
            
            <PasswordBox x:Name="pbConfirm" materialDesign:HintAssist.Hint="Confirm Password" Margin="0 8" Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         PasswordChanged="PbConfirm_PasswordChanged"/>

            <ComboBox materialDesign:HintAssist.Hint="Department" ItemsSource="{Binding Departments}" SelectedItem="{Binding Department}" Margin="0 8"/>
            
            <TextBox materialDesign:HintAssist.Hint="Designation" Text="{Binding Designation}" Margin="0 8" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            
            <TextBox materialDesign:HintAssist.Hint="Name" Text="{Binding FullName}" Margin="0 8" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0 16">
                <Button Content="CREATE" Command="{Binding CreateUserCommand}" Width="100" Style="{StaticResource PrimaryButton}"/>
                <Button Content="CLEAR" Command="{Binding ClearCommand}" Click="Button_Clear_Click" Width="100" Margin="16 0 0 0" Style="{StaticResource SubtleButton}"/>
                <Button Command="{Binding RefreshCommand}" Style="{StaticResource IconButton}" ToolTip="Refresh" Margin="8 0 0 0">
                    <materialDesign:PackIcon Kind="Refresh" Width="14" Height="14"/>
                </Button>
            </StackPanel>
        </StackPanel>

        <!-- Right: Permissions Grid -->
        <DockPanel Grid.Column="1">
            <TextBlock Text="Permissions" DockPanel.Dock="Top" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0 0 0 16"/>
            
            <DataGrid ItemsSource="{Binding Permissions}" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Module Name" Binding="{Binding RoleName}" IsReadOnly="True" Width="*"/>
                    <DataGridCheckBoxColumn Header="Access" Binding="{Binding IsSelected}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
