<UserControl x:Class="ACGCET_Admin.Views.AdminControl.ExtMarkEntryBarcodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:ACGCET_Admin.ViewModels.AdminControl"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ExtMarkEntryBarcodeViewModel}"
             d:DesignHeight="700" d:DesignWidth="800">

    <Grid Background="Transparent" Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1">
            <TextBlock Text="External Mark Entry (Barcode)" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0 0 0 24" HorizontalAlignment="Center"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Max Mark Setting -->
                <TextBlock Text="Max Mark" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" FontWeight="Bold"/>
                <TextBox Text="{Binding MaxMark}" Grid.Row="0" Grid.Column="1" Margin="0 8" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                <!-- Barcode Input -->
                <TextBlock Text="Barcode" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" FontWeight="Bold"/>
                <TextBox Text="{Binding Barcode, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="0 8" Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:HintAssist.Hint="Scan Barcode Here">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding ProcessBarcodeCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <!-- Info Display (ReadOnly) -->
                <TextBlock Text="Register Number" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0" Margin="0 16 0 0"/>
                <TextBox Text="{Binding StudentRegNo}" Grid.Row="2" Grid.Column="1" Margin="0 16 0 0" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Background="#F5F5F5"/>

                <TextBlock Text="Paper Code" VerticalAlignment="Center" Grid.Row="3" Grid.Column="0" Margin="0 8"/>
                <TextBox Text="{Binding PaperCode}" Grid.Row="3" Grid.Column="1" Margin="0 8" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Background="#F5F5F5"/>

                <TextBlock Text="Paper Name" VerticalAlignment="Center" Grid.Row="4" Grid.Column="0" Margin="0 8"/>
                <TextBox Text="{Binding PaperName}" Grid.Row="4" Grid.Column="1" Margin="0 8" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Background="#F5F5F5"/>

                <!-- Mark Input -->
                <TextBlock Text="Mark" VerticalAlignment="Center" Grid.Row="5" Grid.Column="0" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0 16 0 0"/>
                <TextBox Text="{Binding MarkInput, UpdateSourceTrigger=PropertyChanged}" Grid.Row="5" Grid.Column="1" Margin="0 16 0 0" FontSize="16" FontWeight="Bold" Style="{StaticResource MaterialDesignOutlinedTextBox}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SaveMarkCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

            </Grid>

                <TextBlock Text="{Binding StatusMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0 24" FontSize="14" FontWeight="Bold"/>

            <Button Command="{Binding RefreshCommand}" Style="{StaticResource IconButton}" ToolTip="Reset / Refresh" HorizontalAlignment="Center" Margin="0 8">
                <materialDesign:PackIcon Kind="Refresh" Width="14" Height="14"/>
            </Button>

        </StackPanel>
    </Grid>
</UserControl>
