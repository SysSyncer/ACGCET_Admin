<UserControl x:Class="ACGCET_Admin.Views.AdminControl.StudentWiseBarcodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:ACGCET_Admin.ViewModels.AdminControl"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:StudentWiseBarcodeViewModel}"
             d:DesignHeight="700" d:DesignWidth="800">

    <Grid Background="Transparent" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / Filter -->
        <Grid Grid.Row="0" Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Paper Code" Text="{Binding SelectedPaperCode}" Width="250" Margin="0 0 16 0" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

            <Button Grid.Column="1" Content="SEARCH" Command="{Binding SearchCommand}" Style="{StaticResource PrimaryButton}"/>

            <Button Grid.Column="3" Content="PRINT REPORT" Command="{Binding PrintCommand}" Style="{StaticResource SubtleButton}"/>

            <Button Grid.Column="4" Command="{Binding RefreshCommand}" Style="{StaticResource IconButton}" ToolTip="Refresh" Margin="8 0 0 0">
                <materialDesign:PackIcon Kind="Refresh" Width="14" Height="14"/>
            </Button>
        </Grid>

        <!-- Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Students}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Reg No" Binding="{Binding RegNo}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding StudentName}" Width="*"/>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}"/>
                <DataGridTextColumn Header="Dummy Number" Binding="{Binding DummyNumber}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
