<UserControl x:Class="ACGCET_Admin.Views.DataManagement.ManageExternalMarksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:ACGCET_Admin.ViewModels.DataManagement"
             d:DataContext="{d:DesignInstance Type=vm:ManageExternalMarksViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">
    <StackPanel>
        <TextBlock Text="Manage External Marks"
                FontSize="20"
                FontWeight="SemiBold"
                Foreground="#09090B"
                Margin="0 0 0 18"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0"
                    Background="White"
                    BorderThickness="1"
                    BorderBrush="#E4E4E7"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="Enter External Mark"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Foreground="#09090B"
                            Margin="0 0 0 14"/>
                    <Grid Margin="0 0 0 10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                Text="{Binding SearchRegNo, UpdateSourceTrigger=PropertyChanged}"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 md:HintAssist.Hint="Student Reg. No"/>
                        <Button Grid.Column="2"
                                Content="Search"
                                Command="{Binding SearchStudentCommand}"
                                Style="{StaticResource PrimaryButton}"
                                Height="38"/>
                    </Grid>
                    <TextBlock Margin="0 0 0 10"
                            FontSize="12"
                            Foreground="#52525B">
                        <Run Text="Student: "/><Run Text="{Binding FoundStudent.FullName, FallbackValue='—'}"
                                FontWeight="SemiBold"/>
                    </TextBlock>

                    <ComboBox ItemsSource="{Binding PaperList}"
                            SelectedItem="{Binding SelectedPaper}"
                            DisplayMemberPath="PaperCode"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                            md:HintAssist.Hint="Paper"
                            Margin="0 0 0 12"/>
                    <ComboBox ItemsSource="{Binding ExaminationList}"
                            SelectedItem="{Binding SelectedExamination}"
                            DisplayMemberPath="ExamCode"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                            md:HintAssist.Hint="Examination"
                            Margin="0 0 0 12"/>
                    <Grid Margin="0 0 0 12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                Text="{Binding TheoryMark, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 md:HintAssist.Hint="Theory"/>
                        <TextBox Grid.Column="2"
                                Text="{Binding LabMark, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 md:HintAssist.Hint="Lab"/>
                        <TextBox Grid.Column="4"
                                Text="{Binding TotalMark, TargetNullValue=''}"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 md:HintAssist.Hint="Total"
                                IsReadOnly="True"
                                Foreground="#7C3AED"/>
                    </Grid>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Save"
                                Command="{Binding SaveCommand}"
                                Style="{StaticResource PrimaryButton}"
                                Width="100"/>
                        <Button Content="Clear"
                                Command="{Binding ClearCommand}"
                                Style="{StaticResource SubtleButton}"
                                Width="80"
                                Margin="8 0 0 0"/>
                        <Button Content="Delete"
                                Command="{Binding DeleteCommand}"
                                Style="{StaticResource DangerButton}"
                                Width="90"
                                Margin="8 0 0 0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border Grid.Column="2"
                    Background="White"
                    BorderThickness="1"
                    BorderBrush="#E4E4E7">
                <DataGrid ItemsSource="{Binding ExternalMarks}"
                        SelectedItem="{Binding SelectedExternalMark}"
                          AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        IsReadOnly="True"
                        HeadersVisibility="Column"
                        BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"
                                Binding="{Binding ExternalMarkId}"
                                Width="50"/>
                        <DataGridTextColumn Header="Student"
                                Binding="{Binding Student.FullName}"
                                Width="*"/>
                        <DataGridTextColumn Header="Paper"
                                Binding="{Binding Paper.PaperCode}"
                                Width="90"/>
                        <DataGridTextColumn Header="Exam"
                                Binding="{Binding Examination.ExamCode}"
                                Width="100"/>
                        <DataGridTextColumn Header="Theory"
                                Binding="{Binding TheoryMark}"
                                Width="65"/>
                        <DataGridTextColumn Header="Lab"
                                Binding="{Binding LabMark}"
                                Width="55"/>
                        <DataGridTextColumn Header="Total"
                                Binding="{Binding TotalMark}"
                                Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
