<UserControl x:Class="ACGCET_Admin.Views.MissingEntry.MissingResultEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:ACGCET_Admin.ViewModels.MissingEntry"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MissingResultEntryViewModel}"
             d:DesignHeight="700" d:DesignWidth="1024">

    <Grid Background="Transparent" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0 0 0 24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <ComboBox Grid.Column="0" materialDesign:HintAssist.Hint="Exam Session" ItemsSource="{Binding ExamSessions}" SelectedItem="{Binding SelectedSession}" DisplayMemberPath="SessionName" Margin="0 0 16 0"/>
            <ComboBox Grid.Column="1" materialDesign:HintAssist.Hint="Semester" ItemsSource="{Binding Semesters}" SelectedItem="{Binding SelectedSemester}" Margin="0 0 16 0"/>
            <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="Paper Code" ItemsSource="{Binding Papers}" SelectedItem="{Binding SelectedPaper}" DisplayMemberPath="PaperCode" Margin="0 0 16 0"/>

            <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="MISSING" Command="{Binding SearchCommand}" Width="100" Style="{StaticResource PrimaryButton}"/>
                <Button Content="PRINT" Command="{Binding PrintCommand}" Width="100" Margin="16 0 0 0" Style="{StaticResource SubtleButton}"/>
                <Button Command="{Binding RefreshCommand}" Style="{StaticResource IconButton}" ToolTip="Refresh" Margin="8 0 0 0">
                    <materialDesign:PackIcon Kind="Refresh" Width="14" Height="14"/>
                </Button>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ReportData}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Paper Code" Binding="{Binding PaperCode}"/>
                <DataGridTextColumn Header="Reg No" Binding="{Binding RegNo}"/>
                <DataGridTextColumn Header="Student Name" Binding="{Binding StudentName}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <ProgressBar IsIndeterminate="True" Style="{StaticResource MaterialDesignCircularProgressBar}" 
                     Grid.RowSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
